"""Cloudflare Turnstile verification (anti-abuse).

Optional. Wire into landing-page form submissions if you expose a public
sign-up surface. The demonstration uses API-key-as-account auth instead,
so Turnstile is currently unused but the helper is kept for completeness.
"""
# TODO: verify_turnstile(token: str) -> bool
