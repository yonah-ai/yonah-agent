"""Domain-validator override hook (framework base).

This module is a placeholder. Vertical forks override it (or add
sibling modules) with their domain-specific light validators
(criterion measurability, score range, clinical concept consistency,
job-criteria objectivity, ...).
"""
# TODO: vertical-specific enum + Pydantic validators
