"""ArtefactDTO — Pydantic v2 contract for the artefact surface (framework base).

Vertical forks subclass and bind the body shape (rubric / care
protocol / job criteria / ...).
"""
from pydantic import BaseModel


class ArtefactDTO(BaseModel):
    """TODO: fill in fields per the framework paper §5."""

    pass
